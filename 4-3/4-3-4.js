"use strict";
function jadgeUserType(user) {
    switch (user.gender) {
        case 'male':
            var u0 = user; // UserA
            return;
        case 'female':
            var u1 = user; // UserB
    }
}
